# Chunk Sink Client configuration
# This file is sourced by the systemd service

# Command line options for chunk-sink-client
# Run 'chunk-sink-client --help' for available options
#
# Common options:
#   -d, --device <name>     ALSA device name (default: default)
#   -r, --rate <hz>         Sample rate (default: 48000)
#   -c, --channels <n>      Number of channels (default: 2)
#   -s, --server <host>     Chunk sink server address
#   -p, --port <port>       Chunk sink server port
#
CHUNK_SINK_CLIENT_OPTS=""
